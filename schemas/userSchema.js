// const mongoose = require('mongoose')

// const userSchema = mongoose.Schema({
//     id: {type: mongoose.Schema.Types.ObjectId},
//     firstName: {type: String, required: [true, 'Enter a first name']},
//     lastName: {type: String, required: [true, 'Enter a last name']},    
//     email: {type: String, required: [true, 'Enter a email'], unique:true}, 
//     password: {type: String, required: [true, 'Enter a password']}, 
// })

// module.exports = mongoose.model("users", userSchema)